const nextConfig = {
    assetPrefix: '/ued',
    // basePath: '/ued',
    distDir: 'build',
    images: {
        domains: [
            'assets.dtstack.com',
        ],
    },
}

module.exports = nextConfig
